# Configuration needed to access a database. Comments about each line down below

spring.datasource.url=jdbc:postgresql://localhost:5432/movie
spring.datasource.username=
spring.datasource.password= 
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always

# spring.datasource.url=jdbc:postgresql://localhost:5432/movie # Default Postgres port. "movie" is the name of the database
# spring.datasource.username= # datasource.username and datasource.password remain empty as I'm using a Mac and Postgres.app for local development
# spring.datasource.password= 
# spring.jpa.hibernate.ddl-auto=create-drop # Clean state every time we run the application
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect # SQL dialect to be Postgres
# spring.jpa.properties.hibernate.format_sql=true # Format the SQL